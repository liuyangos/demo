
name: Build Custom Bazzite Image
on:
  push:
    branches: [ main ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build Image
        uses: ublue-os/containerbuild@v0.1.7
        with:
          containerfile: Containerfile
